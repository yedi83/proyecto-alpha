# experiments/ — append-only

Cada corrida es `exp-NNN/` con: `config.yaml` (exacta), `resultados/` (métricas + trades), y fecha.
Re-correr con otro parámetro = nuevo exp-NNN. Prohibido sobrescribir o borrar.
El número total de experimentos es dato obligatorio del informe (corrección por pruebas múltiples).
