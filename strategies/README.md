# strategies/ — Biblioteca de Estrategias Validadas

Solo contiene código **congelado** que completó el pipeline de validación.

- Formato: `H-XXX-nombre-v1/` con el código promovido + `VALIDACION.md` (hash del commit de validación, fecha, enlace a la decisión de fase).
- Ninguna carpeta de aquí se edita. Cambios = nueva versión (v2) que repite el pipeline completo, o nueva hipótesis.
- El bot solo ejecuta código de esta carpeta.

**Estado:** vacía — ninguna hipótesis ha completado el pipeline todavía.
